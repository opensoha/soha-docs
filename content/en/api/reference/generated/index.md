---
title: Generated API Reference
description: Generated OpenSoha API operation and schema index from the public contracts artifacts.
---

# Generated API Reference

Generated from `../soha-contracts/openapi/soha-api.yaml` (Soha API 0.1.0).

This page is generated by `npm run api:reference:generate`. Run `npm run api:reference:check` before publishing docs changes.

## Operation Index

### AI Gateway

- `GET /api/v1/ai-gateway/capabilities` - `getAIGatewayCapabilities`. Schemas: `AIGatewayManifestEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.view`
  - Scopes: caller principal; optional AI client context; optional skill context
  - Audit: Records the caller, source, optional AI client, and skill context while returning only permission-filtered capabilities.
  - Examples: Response fixture `../soha-contracts/examples/openapi/gateway-manifest-envelope.json`; HTTP request `GET /api/v1/ai-gateway/capabilities`
- `POST /api/v1/ai-gateway/tools/:toolName/invoke` - `invokeAIGatewayTool`. Schemas: `ToolInvocationRequest`, `ToolInvocationResultEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.invoke`, `owning tool business permission`
  - Scopes: tool grant or access policy; tool-required resource scope such as cluster or namespace; optional AI client and skill binding
  - Audit: Writes the standard audit record and ai_gateway_audit_logs with actor, AI client, skill, tool, risk, redacted request/result, and related IDs.
  - Examples: Request fixture `../soha-contracts/examples/openapi/tool-invocation-request.json`; Response fixture `../soha-contracts/examples/openapi/tool-invocation-result-envelope.json`
- `POST /api/v1/ai-gateway/resources/read` - `readAIGatewayResource`. Schemas: `ResourceReadRequest`, `ResourceReadResultEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.invoke`, `associated capability business permission`
  - Scopes: manifest resource visibility; resource URI or name; resource context such as cluster, namespace, application, or environment; optional AI client and skill binding
  - Audit: Writes Gateway audit for resource reads with caller, AI client, skill, resource identity, request context, and redacted related metadata; the local MCP process only proxies the backend boundary.
  - Examples: HTTP request `POST /api/v1/ai-gateway/resources/read`
- `POST /api/v1/ai-gateway/prompts/get` - `getAIGatewayPrompt`. Schemas: `PromptGetRequest`, `PromptGetResultEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.invoke`, `associated prompt or tool business permission`
  - Scopes: prompt name; prompt arguments and context; optional AI client and skill binding
  - Audit: Writes Gateway audit for prompt reads with caller, AI client, skill, prompt name, arguments/context, and redacted related metadata; prompt content is assembled by the backend.
  - Examples: HTTP request `POST /api/v1/ai-gateway/prompts/get`
- `GET /api/v1/ai-gateway/personal-access-tokens` - `listPersonalAccessTokens`. Schemas: `PersonalAccessTokenListEnvelope`.
  - Status: `implemented`
  - Permissions: `authenticated Gateway principal`, `ai.gateway.manage for cross-subject queries`
  - Scopes: current user by default; optional scope filter; optional userId filter for privileged queries
  - Audit: Returns token metadata and tokenPrefix only; token values and token hashes are never returned by the listing boundary.
  - Examples: HTTP request `GET /api/v1/ai-gateway/personal-access-tokens?scope=default`
- `POST /api/v1/ai-gateway/personal-access-tokens` - `createPersonalAccessToken`. Schemas: `CreatedPersonalAccessTokenEnvelope`, `PersonalAccessTokenInput`.
  - Status: `implemented`
  - Permissions: `authenticated Gateway principal`, `requested permissionKeys must be valid for the caller`
  - Scopes: token scopes; permissionKeys; optional expiresAt; optional metadata
  - Audit: Creates an opaque personal Gateway token and returns the clear value once; persistent storage keeps only the hash and display prefix.
  - Examples: HTTP request `POST /api/v1/ai-gateway/personal-access-tokens`
- `POST /api/v1/ai-gateway/personal-access-tokens/:tokenID/revoke` - `revokePersonalAccessToken`. Schemas: `OperationStatus`.
  - Status: `implemented`
  - Permissions: `token owner`, `ai.gateway.manage for delegated administration`
  - Scopes: personal access token id; current principal ownership or privileged administration
  - Audit: Revokes a personal Gateway token by id, invalidating future bearer use while recording the token lifecycle action without exposing token values or hashes.
  - Examples: HTTP request `POST /api/v1/ai-gateway/personal-access-tokens/:tokenID/revoke`
- `POST /api/v1/ai-gateway/personal-access-tokens/:tokenID/rotate` - `rotatePersonalAccessToken`. Schemas: `CreatedPersonalAccessTokenEnvelope`, `TokenRotationInput`.
  - Status: `implemented`
  - Permissions: `token owner`, `ai.gateway.manage for delegated administration`
  - Scopes: personal access token id; replacement token scopes and permissionKeys; optional expiresAt and metadata
  - Audit: Rotates a personal Gateway token and returns the replacement clear value once; storage keeps only the new hash/display prefix and audit records the lifecycle action.
  - Examples: HTTP request `POST /api/v1/ai-gateway/personal-access-tokens/:tokenID/rotate`
- `GET /api/v1/ai-gateway/service-accounts` - `listServiceAccounts`. Schemas: `ServiceAccountListEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: service identity inventory; roleIds, teamIds, and scopeGrantIds visibility
  - Audit: Returns service-account metadata and ownership boundaries without exposing service-account token values or hashes.
  - Examples: HTTP request `GET /api/v1/ai-gateway/service-accounts`
- `POST /api/v1/ai-gateway/service-accounts` - `createServiceAccount`. Schemas: `ServiceAccountEnvelope`, `ServiceAccountInput`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: service account id and status; roleIds; teamIds; scopeGrantIds; optional owner and metadata
  - Audit: Creates a bounded service identity for automation use while keeping roles, teams, and scope grants explicit for later token evaluation.
  - Examples: HTTP request `POST /api/v1/ai-gateway/service-accounts`
- `GET /api/v1/ai-gateway/service-account-tokens` - `listServiceAccountTokens`. Schemas: `ServiceAccountTokenListEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: service-account token inventory; service account subject; token scopes and permissionKeys
  - Audit: Returns service-account token metadata and tokenPrefix only; clear token values and hashes are excluded from list responses.
  - Examples: HTTP request `GET /api/v1/ai-gateway/service-account-tokens`
- `POST /api/v1/ai-gateway/service-accounts/:serviceAccountID/tokens` - `createServiceAccountToken`. Schemas: `CreatedServiceAccountTokenEnvelope`, `ServiceAccountTokenInput`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: serviceAccountID; token scopes; permissionKeys; optional expiresAt; optional metadata
  - Audit: Creates an opaque service-account Gateway token and returns the clear value once; persistent storage keeps only the hash and display prefix.
  - Examples: HTTP request `POST /api/v1/ai-gateway/service-accounts/:serviceAccountID/tokens`
- `POST /api/v1/ai-gateway/service-account-tokens/:tokenID/revoke` - `revokeServiceAccountToken`. Schemas: `OperationStatus`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: service-account token id; service account subject; automation token lifecycle administration
  - Audit: Revokes a service-account Gateway token by id, invalidating future automation bearer use while recording the lifecycle action without exposing token values or hashes.
  - Examples: HTTP request `POST /api/v1/ai-gateway/service-account-tokens/:tokenID/revoke`
- `POST /api/v1/ai-gateway/service-account-tokens/:tokenID/rotate` - `rotateServiceAccountToken`. Schemas: `CreatedServiceAccountTokenEnvelope`, `TokenRotationInput`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: service-account token id; replacement token scopes and permissionKeys; optional expiresAt and metadata
  - Audit: Rotates a service-account Gateway token and returns the replacement clear value once; storage keeps only the new hash/display prefix and audit records the lifecycle action.
  - Examples: HTTP request `POST /api/v1/ai-gateway/service-account-tokens/:tokenID/rotate`
- `GET /api/v1/ai-gateway/audit-logs` - `listAIGatewayAuditLogs`. Schemas: `AuditLogListEnvelope`, `RiskLevel`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: actor, AI client, skill, tool, approval request, risk, result, and action filters; time range; result limit
  - Audit: Reads the dedicated AI Gateway audit projection with redacted request/result metadata and without raw tool input, tokens, kubeconfigs, environment variables, or raw log bodies.
  - Examples: HTTP request `GET /api/v1/ai-gateway/audit-logs?riskLevel=high&limit=100`
- `GET /api/v1/ai-gateway/approval-requests` - `listAIGatewayApprovalRequests`. Schemas: `ApprovalRequestListEnvelope`, `RiskLevel`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`, `approval candidate or related actor visibility`
  - Scopes: approval id or status; actor, AI client, skill, tool, risk, and strategy filters; time range and limit
  - Audit: Reads approval request summaries and backend-derived approvalTrace metadata without replaying tools or exposing unredacted tool input.
  - Examples: HTTP request `GET /api/v1/ai-gateway/approval-requests?status=pending&limit=100`
- `GET /api/v1/ai-gateway/approval-requests/:requestID/timeline` - `getAIGatewayApprovalTimeline`. Schemas: `ApprovalTimelineEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`, `approval candidate or related actor visibility`
  - Scopes: approval request id; approval routing trace; related workflow or execution task ids; same-request Gateway audit events
  - Audit: Reads the approval request, decision/stage trace, related workflow or task links, and same-request Gateway audit events without mutating approval state.
  - Examples: HTTP request `GET /api/v1/ai-gateway/approval-requests/:requestID/timeline`
- `POST /api/v1/ai-gateway/approval-requests/:requestID/:action` - `decideAIGatewayApprovalRequest`. Schemas: `ApprovalDecisionInput`, `ApprovalDecisionResultEnvelope`.
  - Status: `implemented`
  - Permissions: `approval candidate authorization`, `ai.gateway.manage for authorized Gateway administration`
  - Scopes: requestID; action approve, reject, or cancel; decision comment; approval routing quorum, stage, role, team, and change-window requirements; original grant, policy, skill binding, and business authorization during replay
  - Audit: Records approval decisions and stage transitions; approvals that satisfy routing replay the original tool only after current Gateway and business authorization are rechecked.
  - Examples: HTTP request `POST /api/v1/ai-gateway/approval-requests/:requestID/approve`
- `GET /api/v1/ai-gateway/governance/status` - `getAIGatewayGovernanceStatus`. Schemas: `GovernanceStatusEnvelope`.
  - Status: `implemented`
  - Permissions: `ai.gateway.manage`
  - Scopes: Gateway administration surface; audit aggregation window from 1 to 168 hours
  - Audit: Reads recent Gateway audit aggregates and returns redacted governance summaries, findings, coverage state, and recommendation actions without exposing raw tool input or token values.
  - Examples: HTTP request `GET /api/v1/ai-gateway/governance/status?windowHours=24`

### Auth

- `GET /api/v1/auth/providers` - `listAuthProviders`. Schemas: `AuthProviderListEnvelope`.
  - Status: `implemented`
  - Permissions: `public authentication discovery`
  - Scopes: configured login providers; provider display metadata; no authenticated user scope
  - Audit: Returns configured authentication provider metadata for login discovery without exposing provider secrets or mutating session state.
  - Examples: HTTP request `GET /api/v1/auth/providers`
- `GET /api/v1/auth/login-options` - `getAuthLoginOptions`. Schemas: `LoginOptionsEnvelope`.
  - Status: `implemented`
  - Permissions: `public authentication discovery`
  - Scopes: enabled login methods; provider availability; no authenticated user scope
  - Audit: Returns login option metadata used by clients before authentication; secret material and provider credentials are never returned.
  - Examples: HTTP request `GET /api/v1/auth/login-options`
- `POST /api/v1/auth/login` - `passwordLogin`. Schemas: `AuthResultEnvelope`, `PasswordLoginRequest`.
  - Status: `implemented`
  - Permissions: `valid login credential`
  - Scopes: username/password credential exchange; session token issuance; principal profile bootstrap
  - Audit: Authenticates a user and returns session tokens plus principal metadata; password values are never echoed and failed attempts should remain redacted in logs.
  - Examples: HTTP request `POST /api/v1/auth/login`
- `POST /api/v1/auth/refresh` - `refreshAuthSession`. Schemas: `AuthResultEnvelope`, `RefreshRequest`.
  - Status: `implemented`
  - Permissions: `valid refresh token`
  - Scopes: session refresh; token expiry and rotation policy; principal profile continuity
  - Audit: Refreshes an authenticated session using a refresh token and returns updated token metadata without exposing stored token hashes.
  - Examples: HTTP request `POST /api/v1/auth/refresh`
- `GET /api/v1/auth/oidc/login` - `beginOIDCLogin`.
  - Status: `implemented`
  - Permissions: `public OIDC login entry`
  - Scopes: legacy single-provider OIDC configuration; state generation; redirect to configured identity provider
  - Compatibility: Legacy browser entry retained for deployments that still use the single-provider OIDC settings path; New multi-provider login buttons should prefer /api/v1/auth/login/:providerID/start when a provider id is available
  - Deprecation: Compatibility route only; no removal date is scheduled for 0.1.x, but new clients should prefer the provider-specific start route.
  - Audit: Starts the browser OIDC login redirect flow without exposing client secrets; the callback and exchange steps perform state validation and session issuance.
  - Examples: HTTP redirect `GET /api/v1/auth/oidc/login`
- `GET /api/v1/auth/oidc/callback` - `handleOIDCCallback`.
  - Status: `implemented`
  - Permissions: `valid OIDC state and provider callback`
  - Scopes: authorization code; state parameter; legacy OIDC provider profile and local identity binding
  - Compatibility: Legacy callback retained for existing OIDC provider redirect URLs; The backend still redirects to the same frontend exchange page as provider-specific callbacks
  - Deprecation: Compatibility route only; no removal date is scheduled for 0.1.x, but new provider configurations should use /api/v1/auth/login/:providerID/callback.
  - Audit: Validates the OIDC callback, reconciles the provider identity with existing local roles and organizations, creates a session, and redirects to the console with a short-lived exchange code.
  - Examples: HTTP redirect `GET /api/v1/auth/oidc/callback?code=...&state=...`
- `POST /api/v1/auth/oidc/exchange` - `exchangeOIDCCode`. Schemas: `AuthResultEnvelope`, `OIDCExchangeRequest`.
  - Status: `implemented`
  - Permissions: `valid one-time OIDC or OAuth2 exchange code`
  - Scopes: frontend callback exchange code; session token issuance; principal profile bootstrap
  - Compatibility: Despite the OIDC path name, this is the shared frontend session exchange endpoint for OIDC and OAuth2-style provider callbacks; The route returns the same AuthResultEnvelope as password login and refresh session flows
  - Audit: Consumes the short-lived login exchange code and returns the final session tokens and principal metadata without returning provider credentials or stored token hashes.
  - Examples: HTTP request `POST /api/v1/auth/oidc/exchange`
- `GET /api/v1/auth/login/:providerID/start` - `beginProviderLogin`.
  - Status: `implemented`
  - Permissions: `public configured-provider login entry`
  - Scopes: providerID; enabled OIDC, OAuth2, Feishu, DingTalk, or WeCom provider; state generation and provider redirect URL
  - Compatibility: Preferred multi-provider browser entry for new console integrations; Coexists with the legacy /api/v1/auth/oidc/login route during the 0.1.x compatibility line
  - Audit: Starts the selected provider browser redirect flow after resolving configured provider metadata; provider secrets remain server-side and the callback handles local identity reconciliation.
  - Examples: HTTP redirect `GET /api/v1/auth/login/:providerID/start`
- `GET /api/v1/auth/login/:providerID/callback` - `handleProviderCallback`.
  - Status: `implemented`
  - Permissions: `valid provider callback state`
  - Scopes: providerID; authorization code and state parameter; provider profile, mapped roles, mapped organizations, and local session
  - Compatibility: Preferred callback route for multi-provider OIDC, OAuth2, Feishu, DingTalk, and WeCom login providers; Uses the same frontend exchange-code handoff as the legacy OIDC callback
  - Audit: Handles the selected provider callback, matches or creates the local user identity, applies configured login-time role or organization enrichment, creates a session, and redirects with a short-lived exchange code.
  - Examples: HTTP redirect `GET /api/v1/auth/login/:providerID/callback?code=...&state=...`
- `GET /api/v1/auth/me` - `getCurrentPrincipal`. Schemas: `PrincipalEnvelope`.
  - Status: `implemented`
  - Permissions: `authenticated principal`
  - Scopes: current principal identity; roles and permissions visible to the caller
  - Audit: Returns the current authenticated principal summary without mutating state or exposing bearer token contents.
  - Examples: HTTP request `GET /api/v1/auth/me`
- `GET /api/v1/auth/profile` - `getCurrentUserProfile`. Schemas: `UserProfileEnvelope`.
  - Status: `implemented`
  - Permissions: `authenticated principal`
  - Scopes: current user profile; workspace and UI bootstrap context
  - Audit: Returns current user profile metadata for console bootstrap without exposing token, password, or provider secret material.
  - Examples: HTTP request `GET /api/v1/auth/profile`
- `GET /api/v1/auth/bootstrap` - `getAuthBootstrap`. Schemas: `AuthBootstrapEnvelope`.
  - Status: `implemented`
  - Permissions: `authenticated principal`
  - Scopes: current principal; workspace/menu bootstrap; client feature and authorization context
  - Audit: Returns authenticated bootstrap state used by clients to initialize navigation and permissions without performing mutations.
  - Examples: HTTP request `GET /api/v1/auth/bootstrap`
- `POST /api/v1/auth/logout` - `logoutAuthSession`. Schemas: `RefreshRequest`.
  - Status: `implemented`
  - Permissions: `authenticated session or refresh token context`
  - Scopes: session termination; refresh token invalidation where applicable
  - Audit: Terminates the current session or refresh context without returning secret material; clients should discard local bearer state after success.
  - Examples: HTTP request `POST /api/v1/auth/logout`
- `POST /api/v1/auth/stream-ticket` - `issueStreamTicket`. Schemas: `StreamTicketEnvelope`, `StreamTicketRequest`.
  - Status: `implemented`
  - Permissions: `authenticated principal`
  - Scopes: short-lived stream access; requested stream target; caller authorization for the target stream
  - Audit: Issues a short-lived stream ticket for authorized streaming clients without exposing long-lived bearer tokens to stream URLs.
  - Examples: HTTP request `POST /api/v1/auth/stream-ticket`

### MCP

- `GET /api/v1/mcp/capabilities` - `listMCPCapabilities`. Schemas: `MCPCapabilityListEnvelope`.
  - Status: `implemented`
  - Permissions: `authenticated Gateway or MCP caller context`, `permission-filtered capability visibility`
  - Scopes: MCP adapter catalog; caller principal; optional AI client, source, and skill context
  - Audit: Returns MCP-facing capability metadata for the caller without mutating state; downstream tool/resource/prompt calls perform their own Gateway audit and authorization checks.
  - Examples: HTTP request `GET /api/v1/mcp/capabilities`

### Platform

- `GET /api/v1/clusters/capabilities` - `listClusterCapabilityMatrix`. Schemas: `ClusterCapabilityMatrixEnvelope`.
  - Status: `implemented`
  - Permissions: `authenticated platform principal`
  - Scopes: cluster runtime capability matrix; Direct and Agent connection mode support metadata; risk, approval, required scopes, and documentation links
  - Audit: Returns platform capability metadata only; callers use it to explain Direct and Agent support, risk, approval requirements, required scopes, and documentation links without executing cluster operations.
  - Examples: HTTP request `GET /api/v1/clusters/capabilities`

### Plugins

- `GET /api/v1/plugins/marketplace` - `listMarketplacePlugins`. Schemas: `MarketplacePluginListEnvelope`.
  - Status: `implemented`
  - Permissions: `plugin.view`
  - Scopes: marketplace catalog visibility; optional query, type, and publisher filters; current principal plugin discovery permissions
  - Audit: Returns marketplace plugin metadata visible to the caller without installing assets, granting permissions, or exposing marketplace credentials.
  - Examples: HTTP request `GET /api/v1/plugins/marketplace?type=connector`
- `GET /api/v1/plugins/marketplace/:pluginID` - `getMarketplacePlugin`. Schemas: `MarketplacePluginEnvelope`.
  - Status: `implemented`
  - Permissions: `plugin.view`
  - Scopes: marketplace plugin id; publisher and version metadata; requested permissions and secret requirements preview
  - Audit: Reads marketplace plugin detail for review before installation; requested permissions and secret requirements are descriptive and do not grant access.
  - Examples: HTTP request `GET /api/v1/plugins/marketplace/:pluginID`
- `GET /api/v1/plugins/installed` - `listInstalledPlugins`. Schemas: `InstalledPluginListEnvelope`.
  - Status: `implemented`
  - Permissions: `plugin.view`
  - Scopes: installed plugin inventory; workspace or control-plane plugin visibility; enabled, disabled, and configuration state
  - Audit: Returns installed plugin records and status summaries without exposing secret values referenced by plugin configuration.
  - Examples: HTTP request `GET /api/v1/plugins/installed`
- `POST /api/v1/plugins/install` - `installPlugin`. Schemas: `InstalledPluginEnvelope`, `PluginInstallRequest`.
  - Status: `implemented`
  - Permissions: `plugin.install`, `approval where required by requested permissions or risk`
  - Scopes: marketplace plugin id and version; manifest snapshot; requested permissions, secret requirements, and compatibility metadata
  - Audit: Installs a plugin by snapshotting its manifest, source metadata, checksum or signature status, requested permissions, and risk review outcome; installation does not grant the requested permissions by itself.
  - Examples: HTTP request `POST /api/v1/plugins/install`
- `GET /api/v1/plugins/:pluginID` - `getInstalledPlugin`. Schemas: `InstalledPluginEnvelope`.
  - Status: `implemented`
  - Permissions: `plugin.view`
  - Scopes: installed plugin id; installation status and enabled state; installed version and manifest snapshot metadata
  - Audit: Reads an installed plugin record without returning secret values, mutable authorization grants, or marketplace credentials.
  - Examples: HTTP request `GET /api/v1/plugins/:pluginID`
- `DELETE /api/v1/plugins/:pluginID` - `removeInstalledPlugin`. Schemas: `OperationStatus`.
  - Status: `implemented`
  - Permissions: `plugin.manage`
  - Scopes: installed plugin id; plugin-owned asset cleanup or detachment; preserved installation and audit history
  - Audit: Removes an installed plugin record while preserving lifecycle audit evidence and ensuring plugin-owned assets are cleaned up or detached through controlled paths.
  - Examples: HTTP request `DELETE /api/v1/plugins/:pluginID`
- `GET /api/v1/plugins/:pluginID/manifest` - `getInstalledPluginManifest`. Schemas: `PluginManifestEnvelope`.
  - Status: `implemented`
  - Permissions: `plugin.view`
  - Scopes: installed plugin id; snapshotted plugin manifest; requested permissions, capability refs, and secret requirements
  - Audit: Reads the installed manifest snapshot used for review, drift checks, and upgrade decisions without re-fetching mutable marketplace content as source of truth.
  - Examples: HTTP request `GET /api/v1/plugins/:pluginID/manifest`
- `POST /api/v1/plugins/:pluginID/enable` - `enableInstalledPlugin`. Schemas: `InstalledPluginEnvelope`.
  - Status: `implemented`
  - Permissions: `plugin.manage`
  - Scopes: installed plugin id; enabled state transition; current compatibility, policy, and secret reference readiness
  - Audit: Enables an installed plugin only after current policy and readiness checks; plugin-declared permissions remain subject to RBAC, grants, access policies, skill bindings, approvals, and scopes.
  - Examples: HTTP request `POST /api/v1/plugins/:pluginID/enable`
- `POST /api/v1/plugins/:pluginID/disable` - `disableInstalledPlugin`. Schemas: `InstalledPluginEnvelope`.
  - Status: `implemented`
  - Permissions: `plugin.manage`
  - Scopes: installed plugin id; disabled state transition; dependent assets and capability exposure
  - Audit: Disables an installed plugin and records the state transition without deleting the manifest snapshot, lifecycle history, or required audit trail.
  - Examples: HTTP request `POST /api/v1/plugins/:pluginID/disable`
- `POST /api/v1/plugins/:pluginID/upgrade` - `upgradeInstalledPlugin`. Schemas: `InstalledPluginEnvelope`, `PluginInstallRequest`.
  - Status: `implemented`
  - Permissions: `plugin.manage`, `approval where required by new permissions or risk`
  - Scopes: installed plugin id; target marketplace version or default latest compatible version; refreshed manifest snapshot, checksum or signature status, and compatibility metadata
  - Audit: Upgrades an installed plugin by taking a fresh target-version manifest snapshot and recording changed permissions, secret requirements, compatibility, and risk review outcome.
  - Examples: HTTP request `POST /api/v1/plugins/:pluginID/upgrade`
- `PUT /api/v1/plugins/:pluginID/config` - `configureInstalledPlugin`. Schemas: `InstalledPluginEnvelope`, `PluginConfigRequest`.
  - Status: `implemented`
  - Permissions: `plugin.configure_secrets`
  - Scopes: installed plugin id; configuration fields and secret reference bindings; redacted config update summary
  - Audit: Updates plugin configuration and secret references while redacting secret values from responses, logs, and audit records.
  - Examples: HTTP request `PUT /api/v1/plugins/:pluginID/config`

### Runner

- `POST /api/v1/delivery/execution-tasks/claim` - `claimExecutionTask`. Schemas: `ExecutionTaskClaimRequest`, `ExecutionTaskEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `delivery.execution-tasks.manage when using service-account tokens`
  - Scopes: runner providerKinds; agentID and optional runtimeEndpoint; queued delivery execution tasks visible to the runner
  - Audit: Claims one delivery execution task for an authorized runner and records runner identity, provider kind, runtime endpoint, and heartbeat context without granting access outside the task boundary.
  - Examples: HTTP request `POST /api/v1/delivery/execution-tasks/claim`
- `GET /api/v1/delivery/execution-tasks/:taskID/runner-status` - `getExecutionTaskRunnerStatus`. Schemas: `ExecutionTaskEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `delivery.execution-tasks.manage when using service-account tokens`
  - Scopes: execution task id; runner-visible task state; cancellation, timeout, and terminal status polling
  - Audit: Returns runner-visible task state so long-running agents can observe cancellation or timeout decisions without mutating the task.
  - Examples: HTTP request `GET /api/v1/delivery/execution-tasks/:taskID/runner-status`
- `POST /api/v1/delivery/execution-callbacks` - `recordExecutionCallback`. Schemas: `ExecutionCallbackRequest`, `ExecutionTaskEnvelope`.
  - Status: `implemented`
  - Permissions: `valid execution task callback token`
  - Scopes: task-scoped callback token; execution status and heartbeat; logs, artifacts, and build or deploy backfill metadata
  - Audit: Persists delivery execution callbacks, updates task state, and backfills related build, deploy, release-bundle, log, and artifact records while rejecting stale task-scoped callback ownership.
  - Examples: HTTP request `POST /api/v1/delivery/execution-callbacks`
- `POST /api/v1/docker/operations/claim` - `claimDockerOperation`. Schemas: `DockerOperationClaimRequest`, `DockerOperationEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `docker.operations.manage when using service-account tokens`
  - Scopes: Docker runner operation kinds; worker id and runtime endpoint; queued Docker operations visible to the runner
  - Audit: Claims one Docker operation for an authorized runner so Docker Engine or Compose work stays outside API handlers and returns only the operation payload needed by that runner.
  - Examples: HTTP request `POST /api/v1/docker/operations/claim`
- `GET /api/v1/docker/operations/:operationID/runner-status` - `getDockerOperationRunnerStatus`. Schemas: `DockerOperationEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `docker.operations.manage when using service-account tokens`
  - Scopes: Docker operation id; runner-visible operation state; cancellation, timeout, and terminal status polling
  - Audit: Returns runner-visible Docker operation state so agents can stop local Docker or Compose work when the control plane has already reached a terminal state.
  - Examples: HTTP request `GET /api/v1/docker/operations/:operationID/runner-status`
- `POST /api/v1/docker/operation-callbacks` - `recordDockerOperationCallback`. Schemas: `DockerOperationCallbackRequest`, `DockerOperationEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `operation-scoped callback token`
  - Scopes: Docker operation id; operation-scoped callback token; status, logs, runtime service state, and output metadata
  - Audit: Records Docker runner callbacks and normalizes status, logs, service state, and failure evidence without allowing the runner token to expand Docker operation scope.
  - Examples: HTTP request `POST /api/v1/docker/operation-callbacks`
- `POST /api/v1/copilot/agent-runs/claim` - `claimAgentRun`. Schemas: `AgentRunClaimRequest`, `AgentRunEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `ai.gateway.invoke or observe.ai.chat when using service-account tokens`
  - Scopes: Agent Runtime provider id or provider kind; runner worker id; queued AgentRun records matching the provider
  - Audit: Claims an AgentRun for an authorized provider runner and returns the run contract, toolBindings, skillBindings, scope, and task-scoped callback token needed for controlled execution.
  - Examples: HTTP request `POST /api/v1/copilot/agent-runs/claim`
- `POST /api/v1/copilot/agent-runs/callback` - `recordAgentRunCallback`. Schemas: `AgentRunCallbackRequest`, `AgentRunEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `AgentRun callback token`
  - Scopes: AgentRun id and callback token; running, completed, failed, canceled, or timeout status; analysis artifacts, tool executions, usage, and data-source snapshots
  - Audit: Records Agent Runtime callbacks, heartbeats, provider usage, tool execution summaries, and normalized AnalysisArtifact output while redacting provider secrets and raw credential material.
  - Examples: HTTP request `POST /api/v1/copilot/agent-runs/callback`
- `POST /api/v1/copilot/agent-runs/tool-call` - `recordAgentRunToolCall`. Schemas: `AgentRunToolCallRequest`, `AgentToolCallResultEnvelope`.
  - Status: `implemented`
  - Permissions: `runtime execution runner token`, `AgentRun callback token`, `read-only tool binding captured on the AgentRun`
  - Scopes: AgentRun id and callback token; toolBindings snapshot; read-only tool input, scope, and result redaction policy
  - Audit: Executes only read-only tools from the AgentRun toolBindings snapshot, records each ToolExecution, and prevents external providers from bypassing Soha data-source credentials, resource scopes, or Gateway redaction.
  - Examples: Request fixture `../soha-contracts/examples/openapi/agent-run-tool-call-request.json`; Response fixture `../soha-contracts/examples/openapi/agent-tool-call-result-envelope.json`
- `GET /api/v1/copilot/agent-runs` - `listAgentRuns`. Schemas: `GenericItemsEnvelope`.
  - Status: `implemented`
  - Permissions: `observe.ai.chat`
  - Scopes: current user or authorized AI workbench visibility; AgentRun status and provider filters where supported; redacted output and artifact summaries
  - Audit: Lists AgentRun records visible to the current user for AI Workbench operations without exposing runner callback tokens or provider credentials.
  - Examples: HTTP request `GET /api/v1/copilot/agent-runs`

### System

- `GET /api/v1/healthz` - `getHealthz`. Schemas: `GenericObject`.
  - Status: `implemented`
  - Permissions: `public health probe`
  - Scopes: process liveness; no workspace, cluster, or user scope
  - Audit: Liveness probing is intentionally lightweight and does not emit per-request audit records; production exposure is controlled by deployment routing.
  - Examples: HTTP request `GET /api/v1/healthz`
- `GET /api/v1/readyz` - `getReadyz`. Schemas: `GenericObject`.
  - Status: `implemented`
  - Permissions: `public readiness probe`
  - Scopes: service readiness; dependency readiness summary; no workspace, cluster, or user scope
  - Audit: Readiness probing returns operational state for orchestrators and release gates without exposing secrets or emitting per-request audit records.
  - Examples: HTTP request `GET /api/v1/readyz`

## JSON Schema Artifacts

- `agent/agent-protocol.schema.json` - Soha Agent Protocol.
  - ID: `https://contracts.opensoha.dev/agent/agent-protocol.schema.json`
- `auth/token-claims.schema.json` - Soha Token Claims.
  - ID: `https://contracts.opensoha.dev/auth/token-claims.schema.json`
  - Required: `sub`, `exp`, `iat`, `tokenId`, `tokenKind`, `subjectType`, `subjectId`
- `capabilities/cluster-capability-matrix.schema.json` - OpenSoha Cluster Capability Matrix.
  - ID: `https://contracts.opensoha.dev/capabilities/cluster-capability-matrix.schema.json`
  - Required: `items`
- `connectors/connector-event-envelope.schema.json` - OpenSoha Connector Event Batch Envelope.
  - ID: `https://contracts.opensoha.dev/connectors/connector-event-envelope.schema.json`
  - Required: `connectorId`, `events`
- `events/event-envelope.schema.json` - Soha Event Envelope.
  - ID: `https://contracts.opensoha.dev/events/event-envelope.schema.json`
  - Required: `id`, `source`, `category`, `severity`, `summary`
- `governance/approval-request.schema.json` - OpenSoha Approval Request.
  - ID: `https://contracts.opensoha.dev/governance/approval-request.schema.json`
  - Required: `id`, `status`, `strategy`, `actorType`, `actorId`, `toolName`, `riskLevel`, `requiresApproval`, `summary`, `createdAt`, `updatedAt`
- `governance/audit-log.schema.json` - OpenSoha Audit Log.
  - ID: `https://contracts.opensoha.dev/governance/audit-log.schema.json`
  - Required: `id`, `actorType`, `actorId`, `action`, `result`, `summary`, `createdAt`
- `mcp/gateway-manifest.schema.json` - Soha AI Gateway Manifest.
  - ID: `https://contracts.opensoha.dev/mcp/gateway-manifest.schema.json`
  - Required: `name`, `version`, `generatedAt`, `principal`, `caller`, `permissionKeys`, `tools`, `summary`
- `plugins/plugin-manifest.schema.json` - OpenSoha Plugin Manifest.
  - ID: `https://contracts.opensoha.dev/plugins/plugin-manifest.schema.json`
  - Required: `id`, `name`, `version`, `publisher`, `type`
- `presets/mcp-preset.schema.json` - OpenSoha MCP Preset.
  - ID: `https://contracts.opensoha.dev/presets/mcp-preset.schema.json`
  - Required: `schemaVersion`, `id`, `name`, `version`, `description`, `riskLevel`, `skillRefs`, `platformCapabilityRefs`, `requiredScopes`, `tools`, `guardrails`
- `profiles/agent-profile.schema.json` - OpenSoha Agent Profile.
  - ID: `https://contracts.opensoha.dev/profiles/agent-profile.schema.json`
  - Required: `schemaVersion`, `id`, `name`, `version`, `description`, `mode`, `providerKind`, `skillRefs`, `mcpPresetRefs`, `capabilityRefs`, `platformCapabilityRefs`, `requiredScopes`, `enabledToolRefs`, `guardrails`
- `runner/operation-lifecycle.schema.json` - OpenSoha Runner Operation Lifecycle.
  - ID: `https://contracts.opensoha.dev/runner/operation-lifecycle.schema.json`
  - Required: `operationId`, `operationKind`, `runnerId`, `state`, `occurredAt`
- `skills/skill-manifest.schema.json` - Soha Skill Manifest.
  - ID: `https://contracts.opensoha.dev/skills/skill-manifest.schema.json`
  - Required: `id`, `name`, `category`
